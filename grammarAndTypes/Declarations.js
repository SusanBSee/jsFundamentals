
let a = 2;
/*1 (COMMENT!) let = 1   a = 2   = = 3   r; = 2
         - let is the keyword
         - = is the assignment operator
        Javascript keyword to denote the creation of a variable
        the name of the variable
        assignment operator
        The initial value
                /* OR
        /* can use /* to denote a comment 
        which will make all lines after as a comment until closing with
        */
let x = 10;  //we have declared & initialized the variable

/*Declaration => This is where the variableis created by utilizing designated JS keywords(var, let, and const)
Initialization => This is where the developer sets the value for the declared variable*/

let y;
console.log('Declaration', y);

y = 10;
console.log('Initizalization', y);

/* Console = >an object that can give us access to a browser's debugging console
We use the console to check the values and variables of things we create in our code*/


console.log("This is a message");
